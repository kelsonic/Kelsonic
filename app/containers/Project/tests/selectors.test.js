// import { fromJS } from 'immutable';
// import { makeSelectProjectDomain } from '../selectors';

// const selector = makeSelectProjectDomain();

describe('makeSelectProjectDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
