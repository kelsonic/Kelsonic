<div class="container">
  <div class="section">

    <!--   Icon Section   -->
    <div class="row">
      <div class="col s12">
        <div class="icon-block">
          
          <!--Header portfolio-->
          <div class="center">
            <h2 class="orange-text text-darken-2">My Projects</h2>
            <p class="grey-text">
              I created the following projects by consulting countless online references,
              online bootcamps, actual bootcamps, and many books.
            </p>
          </div>
          
          <!-- Projects -->
          <% @projects.each do |project| %>
            <hr/>
            <br/>
            <%= render 'project', project: project %>
          <% end %>
          
        </div>
      </div>
    </div>
  </div>
</div>